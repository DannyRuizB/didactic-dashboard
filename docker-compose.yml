services:
  dashboard:
    build: .
    container_name: didactic-dashboard
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - DB_PATH=/app/data/dashboard.db
      - PING_INTERVAL=10000
    restart: unless-stopped
