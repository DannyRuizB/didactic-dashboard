# Example only — deploy.sh generates the real inventory.ini from
# `terraform output` after the instance is up.
#
# ansible_user must be set HERE (not with -u): the ssh_hardening lockout
# guard checks {{ ansible_user }}'s authorized_keys before disabling
# password authentication.
[dashboard]
203.0.113.10 ansible_user=admin ansible_ssh_private_key_file=~/.ssh/id_ed25519
